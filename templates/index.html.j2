<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ name }} - Resume</title>
    <meta name="description"
          content="Resume and CV for {{ name }}{{ ', ' + title if title else '' }}">
    <meta name="author" content="{{ name }}">
    <!-- Open Graph for social sharing -->
    <meta property="og:title" content="{{ name }} - Resume">
    <meta property="og:type" content="profile">
    <meta property="og:description"
          content="Professional resume for {{ name }}">
    <link rel="stylesheet" href="style.css">
    <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .container {
      background: white;
      border-radius: 12px;
      padding: 60px 40px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
      text-align: center;
      max-width: 500px;
    }
    h1 {
      color: #333;
      font-size: 2.5em;
      margin-bottom: 10px;
    }
    .subtitle {
      color: #666;
      font-size: 1.2em;
      margin-bottom: 10px;
    }
    p {
      color: #666;
      margin-bottom: 30px;
    }
    .links {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    a {
      color: white;
      text-decoration: none;
      padding: 15px 30px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 8px;
      display: block;
      font-weight: 600;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    a:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }
    .secondary {
      background: #f0f0f0;
      color: #333;
    }
    .version {
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #eee;
      color: #aaa;
      font-size: 0.85em;
      font-family: 'Monaco', 'Courier New', monospace;
    }
    .version a {
      color: #667eea;
      background: none;
      padding: 0;
      display: inline;
      font-weight: normal;
      font-size: 1em;
    }
    .version a:hover {
      transform: none;
      box-shadow: none;
      text-decoration: underline;
    }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>{{ name }}</h1>
      {% if title %}<p class="subtitle">{{ title }}</p>{% endif %}
      <p>Download my resume in your preferred format:</p>
      <div class="links">
        <a href="resume.pdf">PDF Resume (One Page)</a>
        <a href="resume.html" class="secondary">HTML Resume</a>
        <a href="resume.md" class="secondary">Markdown Source</a>
      </div>
      <div class="footer">
        {% set contact_keys = ["email", "linkedin", "github"] %}
        <p>
          {% for key, value in contact.items() %}
            {% if key in contact_keys and value %}
              <a href="{% if key == 'email' %}mailto:{% endif %}{{ value }}"
                 class="footer-link">{{ key|title }}</a>
            {% endif %}
          {% endfor %}
        </p>
      </div>
    {% if build_info %}
      <div class="version">
        <p>
          Version: <a href="{{ build_info.release_url }}" target="_blank" rel="noopener">{{ build_info.tag }}</a>
          <br>
          Built: {{ build_info.date }}
          <br>
          Commit: <a href="{{ build_info.commit_url }}" target="_blank" rel="noopener">{{ build_info.commit_short }}</a>
        </p>
      </div>
    {% endif %}
  </div>
</body>
</html>